package net.opentrends.marvelheroes;

import android.content.Context;
import android.support.v4.app.FragmentActivity;

import java.util.ArrayList;
import java.util.List;

/**
 * Created by sfcar on 28/01/2017.
 */

/*public class MarvelComicSingleton { //Almacenamos la lista de comics en el Singleton

    private static MarvelComicSingleton sMarvelComicSingleton;
    private List<MarvelComic> mMarvelComics;

    public static MarvelComicSingleton getInstance(Context context) {//Context para la BBDD
        if(sMarvelComicSingleton==null){
            sMarvelComicSingleton = new MarvelComicSingleton(context); //Si no hay una instancia la crea
        }
        return sMarvelComicSingleton;
    }

    private MarvelComicSingleton(Context context) { //Constructor privado, solo se puede acceder desde getInstance

        }


    public List<MarvelComic> getMarvelComics() { //Acceder al listado de Heroes
        return mMarvelComics;
    }

//    public MarvelComic getMarvelComic(int id){ //Devuelve los comics con el ID del heroe indicado
//        for(MarvelComic marvelComic : mMarvelComics){
//            if(marvelComic.getIdHeroe().getId() == id){
//                return marvelComic;
//            }
//        } return null;
//    }

}
*/